{
  "name": "atlasoc",
  "private": true,
  "scripts": {
    "build": "esbuild src/main.jsx --bundle --minify --outfile=public/main.js --platform=browser",
    "dev": "esbuild src/main.jsx --bundle --outfile=public/main.js --platform=browser --watch",
    "test": "node --test tests/**/*.test.mjs"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "esbuild": "^0.25"
  },
  "allowScripts": {
    "esbuild@0.25.12": true
  }
}
