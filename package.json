{
  "name": "atlasoc",
  "private": true,
  "scripts": {
    "build": "esbuild src/main.jsx --bundle --minify --outfile=main.js --platform=browser",
    "dev": "esbuild src/main.jsx --bundle --outfile=main.js --platform=browser --watch"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "esbuild": "^0.25"
  }
}
